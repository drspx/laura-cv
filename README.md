Install dependencies to render locally in neovim:
```
brew install nvim
brew install font-open-sans
brew install font-fontawesome
```
dependencies are needed to make nvim typst preview

Optional packages:
```
brew install typst

```
